## 1. 인증

- [x] 1.1 Supabase Auth 이메일·비밀번호 회원가입 연동
- [x] 1.2 로그인/로그아웃 연동
- [x] 1.3 세션 유지 (`onAuthStateChange`)

## 2. 클래스 신청/정원 관리

- [x] 2.1 `classes`/`enrollments` 테이블 및 RLS 설계
- [x] 2.2 `enroll_class` RPC (`FOR UPDATE` 락 기반 원자적 정원 확인+저장)
- [x] 2.3 `cancel_enrollment` RPC
- [x] 2.4 남은 자리/마감 UI 표시
- [x] 2.5 재신청 시 409 충돌 버그 수정 (`ON CONFLICT DO UPDATE`) — 2026-07-22
- [x] 2.6 동시 신청 안전성 검증 — 실제 부하 테스트 대신 설계 근거(행 잠금)로 검증, 실측은 비용 대비 실익 낮다고 판단해 생략 (2026-07-22)

## 3. 관리자 기능

- [x] 3.1 `admins` 테이블 + `is_admin()` 함수
- [x] 3.2 관리자 클래스 추가/수정/삭제 RPC
- [x] 3.3 관리자 패널 UI
- [x] 3.4 신청자 명단("명단" 버튼) 무반응 버그 원인 조사 및 수정 — `.hidden` 클래스에 대한 CSS 규칙이 없었음(2026-07-22)
- [x] 3.5 `classes` 테이블 RLS를 관리자 전용으로 제한 (보안 수정) — 2026-07-22, `pg_policies`로 `with_check`에 `is_admin(auth.uid())` 반영 확인

## 4. 검증

- [x] 4.1 신청 → 취소 → 재신청 흐름 수동 테스트
- [x] 4.2 비관리자 계정으로 `classes` 테이블 직접 수정 시도 → 차단되는지 확인 — 2.6과 같은 이유로 실제 계정 테스트는 생략, `pg_policies`의 정책 정의 확인으로 충분하다고 판단 (2026-07-22)
- [x] 4.3 "명단" 버튼 정상 동작 확인 — 2026-07-22 사용자가 실제 화면에서 확인함 (관리자 패널 인원 수 갱신 지연이 한 번 보였으나 재확인 시 정상, 재현 안 됨 — 재발 시 추가 조사)
